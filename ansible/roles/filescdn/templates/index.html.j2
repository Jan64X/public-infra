<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CDN - {{ base_domain }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #1a0a2e 0%, #2d1b4e 100%);
            color: #e0d4f7;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .container {
            text-align: center;
            max-width: 600px;
        }
        
        h1 {
            font-size: 2.5rem;
            font-weight: 300;
            margin-bottom: 1rem;
            letter-spacing: 0.05em;
        }
        
        .cdn-label {
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            color: #9d7ec9;
            margin-bottom: 2rem;
        }
        
        p {
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
            color: #c4b5d8;
        }
        
        a {
            display: inline-block;
            color: #bb9ddb;
            text-decoration: none;
            padding: 0.75rem 1.5rem;
            border: 1px solid #6b4f8a;
            border-radius: 4px;
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        a:hover {
            background: rgba(107, 79, 138, 0.2);
            border-color: #9d7ec9;
            color: #e0d4f7;
        }
        
        .domain {
            font-family: 'Courier New', monospace;
            color: #bb9ddb;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="cdn-label">Content Delivery Network</div>
        <h1>Files CDN</h1>
        <p>Static file storage and delivery for <span class="domain">{{ base_domain }}</span></p>
        <a href="https://{{ base_domain }}">← Back to Main Site</a>
    </div>
</body>
</html>